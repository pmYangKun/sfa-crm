# 当前状态（更新于 2026-08-20）

> 高频变动文件。有进展就改这里；**变成"已发生的事"就往 `log.md` 沉。**

---

## 一、工程进度

**全部 5 个 spec 已收口并合入 master，公网跑最新版。**

| spec | 内容 | tag → commit |
|---|---|---|
| 001 | 登录页双栏 + 移动端 + Onboarding | 含在 `v-spec002` |
| 002 | 公网部署安全 / 治理硬化 | `v-spec002` → `2497831` |
| 003 | MEDDICC 销售视角 | `v-spec003` → `cd8133c` |
| 004 | MEDDICC 经理视角 Pipeline | `v-spec004` → `8271812`（PR #5） |
| 005 | MCP 开放平台（只读 MCP Server + `/open` 站点） | `v-spec005` → `0406ec3` |

最近一次改动：**2026-08-20 登录页加 MCP 开放平台入口**（`42a217e`，已 push master + 部署生产，纯前端、跳过 init_db）。

**测试态势（2026-08-16 终态）：** Backend **223 pytest** ／ 开放平台 e2e PC 9 + Mobile 8 ／ 全量 Playwright **91 passed**（1 例 us1 角色切换在满套件下偶发抖动，单独跑通过，与 spec 005 代码路径无关）。

---

## 二、明确推后的事（连同触发条件）

| 事项 | 为什么推后 | 什么时候做 |
|---|---|---|
| MCP 的 **Skill** | 只读扁平工具集没有跨工具流程要编排，撑不起说明书 | **启动写操作那一版**，届时同期交付 |
| MCP 的 **CLI** | —— | 下期；方向 = 主动侧 `watch` / `report`（原"AI 主动巡检每日早报"并入此处） |
| spec 002 T035 后端 LLM 全代理 | env-var 路径已满足 FR-029/030/031 三条硬安全要求 | 真正需要"在线 rotate 多 provider key 不重启"时 |
| `/open` 回 CRM 的反向入口 | 2026-08-20 只做了单向 | 待定 |
| "登录页 → /open"链路进 e2e | 同上 | 待定 |

---

## 三、下一步（内容与工程都指向同一件事）

**下一集（S2E09）要动手把本体论真建一遍**，这是外传三篇留下的作业：

1. 把**对象层和关系层**从代码里搬出来，声明成独立的一层（线索是什么、它跟联系人/跟进记录之间的"边"长什么样）——**自动派生，绝不手写**
2. 加一个遍历工具（从哪个对象出发、沿哪条边走、筛什么）
3. **逐跳鉴权，直接复用现有 DataScope，不新建权限体系**
4. 然后跟现在这套接口语义层**同场跑同一批业务问题做对比**

**头一个要重问的问题：「王小明离职，交接风险到底有多大」**——上次 agent 停在第一跳（详见 `context/内容策略.md`）。

> 判断依据和已被推翻的论证，全部记在 `context/内容策略.md` §本体论，**动手前必读，别重复已经驳倒的说法。**

---

## 四、常用坐标

- LLM API Key：`src/backend/.env`（dev）／ DB Fernet 密文（生产，spec 002）
- 演示案例：`docs/copilot-cases.md`（8 个独立案例）
- 演示账号：`admin` / `sales01`（王小明）/ `sales02`（李思远）/ `sales03`（张磊）/ `manager01`（陈队长），密码均 `12345`；**admin 生产密码已改，不是 12345**
- 一键启动 `start.bat` ／ 一键重置 `reset-demo.bat` ／ 部署 `docs/deploy.md`
- SQLite 库在 `/var/lib/sfa-crm/sfa_crm.db`，**不在 `/opt/sfa-crm` 内**——整目录换版不碰数据
